import 'dart:core';


main(List<String> args) {
  // DateTime date = DateTime.utc(2020, 05, 27, 01, 43, 09);

  // print(DateTime.now().toUtc().difference(date).inMilliseconds);
  // print(DateTime.now().toUtc().difference(date).inSeconds);
  // print(DateTime.now().toUtc().difference(date).inMinutes);
  // print(DateTime.now().toUtc().difference(date).inHours);
  // print(DateTime.now().toUtc().difference(date).inDays);

  DateTime otherDate = DateTime.now();
  print(otherDate.toString());
}